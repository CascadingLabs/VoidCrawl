---
name: repo-orientation
description: Use when starting work in VoidCrawl or mapping Rust core, PyO3 bindings, Python package, MCP server, tests, or docs before editing.
---
# VoidCrawl Repo Orientation
Read `AGENTS.md` first, then inspect the relevant Rust/Python boundary.

Focus on:
- Rust workspace layout
- PyO3 bridge rules
- browser pool architecture
- Python package and type stubs
- MCP server surface

Output: task-relevant repo summary, likely files, cross-language boundaries, commands, and hazards.
